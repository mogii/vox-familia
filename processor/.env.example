# 复制为 .env 后填上你自己的值。.env 不会被提交。

# --- 大模型（提价格 / 写标题和描述），走 Anthropic Messages 接口 ---
# 这里填 API key（官方 Claude 或 Moonshot/Kimi 都填到这一项）
ANTHROPIC_API_KEY=填你的key

# 用官方 Claude：上面填 sk-ant-... 即可，下面两行不用动。
# 用 Moonshot/Kimi（Anthropic 兼容端点）：上面填你的 Moonshot key，并取消下面两行注释。
#   美国/国际用 .ai，国内用 .cn
# LLM_BASE_URL=https://api.moonshot.ai/anthropic
# CLAUDE_MODEL=kimi-k2.5

# 官方 Claude 默认模型 claude-sonnet-4-6，一般不用改。
# CLAUDE_MODEL=claude-sonnet-4-6

# --- 本地 Whisper（语音转文字）---
# tiny / base / small / medium / large-v3
WHISPER_MODEL=small
# 有 NVIDIA 显卡可改 cuda；Mac 上一般用 auto(=cpu/int8)
# WHISPER_DEVICE=auto

# --- 切段暗号（逗号分隔；不填用默认: 下一件,下一个,下一样,下一款）---
# TRIGGERS=下一件,换一个

# --- 查市场价的暗号（说了这句才会联网搜该件的新品参考价；默认「查原价」）---
# PRICE_TRIGGERS=查原价,查个价
# 联网搜索走 Moonshot/Kimi 的 $web_search（OpenAI 兼容接口），默认复用上面的 key。
# 用官方 Claude 的话这里要单独填一把 Moonshot key 才有搜索：
# SEARCH_API_KEY=你的Moonshot key
# SEARCH_BASE_URL=https://api.moonshot.ai/v1
# SEARCH_MODEL=kimi-k2.5

# --- 每件默认勾几张图（网页里可增减）---
# FRAMES_PER_ITEM=5

# --- 商品里默认写的联系方式（你的微信号）---
CONTACT=你的微信号

# --- Web 服务（server.py）---
# 0.0.0.0 = 同 Wi-Fi 都能访问；只允许 localhost 改成 127.0.0.1
SERVER_HOST=0.0.0.0
SERVER_PORT=8000
# 共享密钥；手机端的快捷方式里要带上这个 token。留空 = 不校验（仅推荐在你完全可控的家庭 Wi-Fi 上）
SERVER_TOKEN=随便填一个长一点的字符串
# 处理过的视频和图片放哪儿（相对 processor/ 目录）
JOBS_DIR=jobs

# --- 成品图字体（中文必须）---
# Mac 自带的字体里挑一个；多个用 : 分隔，按顺序找第一个存在的
# POSTER_FONT=/System/Library/Fonts/PingFang.ttc:/System/Library/Fonts/Hiragino Sans GB.ttc
